import { Layout, Typography } from 'antd';

const { Text } = Typography;
const { Footer } = Layout;
const Footer_ = () => {
    return (
        <Footer style={{ textAlign: 'center', padding: '20px 0' }}>
            <Text>Decentralized CDN ©2024</Text>
        </Footer>
    );
}
export default Footer_ 
